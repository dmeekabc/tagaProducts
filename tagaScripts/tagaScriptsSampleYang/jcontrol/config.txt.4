security {
    policies {
      from-zone NETA to-zone NETB {
        policy deny-All {
          match {
            source-address any;
            destination-address any;
            application any;
          } # match
          then {
              deny;
          } # then
        } # policy
      } # from-zone
    }  # policies
} # security
